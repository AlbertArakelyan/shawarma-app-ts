import formFieldNames from './formFieldNames';

const { orderDetailsFormFieldNames: { size, exceptions } } = formFieldNames;

const orderDetailsFormNames = [size, exceptions];

export default orderDetailsFormNames;
